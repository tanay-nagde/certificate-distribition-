{
  "name": "auth",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote --port 3000",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "bcryptjs": "^3.0.2",
  "hono": "^4.9.1",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}